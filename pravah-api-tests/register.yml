info:
  name: register
  type: http

http:
  method: POST
  url: http://localhost:3000/api/v1/auth/register
  body:
    type: json
    data: |-
      {
        "email": "raj@pravah.com",
        "username": "rajribadiya",
        "password": "supersecretpassword"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
