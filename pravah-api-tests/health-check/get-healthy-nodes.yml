info:
  name: get-healthy-nodes
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3000/api/v1/admin/health/nodes
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
